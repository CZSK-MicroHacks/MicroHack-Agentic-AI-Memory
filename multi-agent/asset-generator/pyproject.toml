[project]
name = "asset-generator"
version = "0.1.0"
description = "Generate custom icons for Multi-Agent Travel Planner UI using Azure OpenAI gpt-image-1.5"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.82.0",
    "azure-identity>=1.21.0",
    "pillow>=11.2.1",
    "python-dotenv>=1.1.0",
]
