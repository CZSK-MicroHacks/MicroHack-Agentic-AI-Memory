{# TODO: Challenge 03 — Write a summarisation prompt for the conversation memory agent.
   This prompt instructs the LLM to produce a concise summary of a customer support
   conversation. The summary will be stored in PostgreSQL with a vector embedding
   for semantic search.

   Consider:
   - What information should be captured (topics, entities, resolution)?
   - How long should the summary be?
   - Should PII (names, emails, phone numbers) be removed or retained?
   - What tone/style works best for downstream semantic search?
#}
