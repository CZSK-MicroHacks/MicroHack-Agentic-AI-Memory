{# TODO: Challenge 04 — Write the user profile injection template.
   This template receives a `user_profile` variable (dict) and should
   render the user's stored profile data into the system prompt.

   Available sections: basic_info, interests, habits, preferences, status, facts.
   Use Jinja2 conditionals to only render sections that have data.

   See challenges/04-user-memory/README.md for requirements. #}
